export const CONFIG = {
    // Use 10.0.2.2 for Android Emulator to access localhost on the host machine
    BACKEND_URL: "http://192.168.31.44:5000/api",
    // Change to your machine's IP address if testing on a physical device
};
